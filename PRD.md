# PRD — Perp_Machine

> 제품 요구사항 (심사 Source of Truth). 기술은 [`TRD.md`](./TRD.md).
> 이 문서에 없는 기능은 존재하지 않는다.
> 2026-08-22 12:15. 팀 `Perp_Machine` (matdaaiga-kr/matdathon-2026#132).

| 항목 | 값 |
|---|---|
| 제품명 | Perp_Machine |
| 주제 | 개인 생산성 향상 앱 — 거시 국면에서 **지금 무엇을 착각하기 쉬운지**를 30초에 본다 |
| 라이브 | https://righthon-hale.azurewebsites.net (Node 보험). 채점 스택은 Python 앱으로 교체 예정 |
| 인증 | **없음.** URL만으로 즉시 사용. 로그인·계정·OAuth 금지 (있으면 전 항목 1점) |
| 기능 수 | **2** (상한 3). F3 없음 |

## 1. 한 줄 정의

공개 거시 지표를 **요청 시** 가져와, 달리오의 lender-creditor / borrower-debtor **양쪽** 제안을 한국어로 돌려주는 에이전트.
올웨더 한 장을 권하지 않는다. 매수·매도 종목 추천을 하지 않는다.

## 2. 문제

거시 뉴스를 볼 때마다 계좌를 만지고 싶어지는 개인은, 자기 포지션이 **채권자인지 채무자인지**를 앱에 말할 수 없다(로그인 없음).
그런데 같은 실질금리가 한쪽엔 매력이고 다른 쪽엔 비용이다 (Dalio, *How Countries Go Broke* — 금리가 물가보다 높으면 lender-creditor가 유리).

올웨더/60-40을 모두에게 권하면 이 프로젝트는 필요 없다.
할 일 정리 앱은 이 문제가 아니다.

생산성 = **어느 나라·어느 국면에서 현금/부채가 어느 쪽에 유리한지**를 조회·점수·양면 제안으로 닫아, 충동적 리밸런싱을 줄인다.

## 3. 사용자와 시나리오

**1차.** 국가·통화를 떠올리며 “지금 돈을 들고 있는 게 손해인가, 빚을 진 게 손해인가”를 묻는 개인.

**2차(심사).** 로그인 없이 GET / 와 `POST /api/chat`.

### A — 국가 한 곳

1. 로그인 화면 없음.
2. `한국 실질금리` 또는 `US vs JP` 를 넣는다.
3. 도구가 공개 API를 친다.
4. 답이 **숫자 출처 + 채권자 제안 + 채무자 제안**을 같이 보여 준다. 하나만 고르라고 하지 않는다.

### B — 심사

`POST /api/chat` `{"message":"한국 실질금리"}` → 200 + 비공백 한국어. 가입 없음.

## 4. 기능

### F1. 달리오 계기판 조회 — **구현 대상**

요청 국가(기본: KR, US, JP, DE, CN, GB, BR)에 대해 아래 지표를 도구로 가져온다.

| 지표 | 달리오에서의 역할 | 당일 소스 | 키 |
|---|---|---|---|
| 실질금리 | 채권자/채무자 유불리를 가른다 | World Bank `FR.INR.RINR` | 없음 |
| CPI 인플레 | 명목−물가의 분모 | WB `FP.CPI.TOTL.ZG` | 없음 |
| GDP 성장 | 단기 사이클 위치 | WB `NY.GDP.MKTP.KD.ZG` | 없음 |
| 정부부채/GDP | BC Ch.17 장기 채무 위험 | WB `GC.DOD.TOTL.GD.ZS` | 없음 |
| 외환보유 | BC Ch.17 완충 | WB `FI.RES.TOTL.CD` | 없음 |
| 민간신용 갭·DSR | BDC 거품/상환능력 | BIS (키 없음) | 없음 |
| 정책금리 | 명목 금리 | ECB SDMX (유로) / 여타 WB·BIS | 없음 |

시차: WB는 **연간·후행**이다. UI/프롬프트에 빈티지를 밝힌다. 실시간 지휘봉이 아님 (달리오도 부채/GDP 평균을 위기 징후가 아니라고 했다).

**안 하는 소스:** IMF(77단 실측 중단). FRED/ECOS(키)는 F1에 필수 아님. yfinance는 F1 아님(아래 비목표).

### F2. 양면 제안 (lender-creditor \| borrower-debtor) — **구현 대상**

같은 숫자로 두 단락을 항상 낸다.

- **채권자:** 실질금리가 높은 나라에서 명목 현금·채권이 매력적인지, 물가에 지는지.
- **채무자:** 같은 나라에서 부채 실질비용이 싼지/비싼지.

한쪽에 “올웨더로 사세요”를 붙이지 않는다.
위험한 행동 전 확인: 매매 문장이 나오면 모델이 거절한다.

### F3

없음. 여유 있으면 같은 화면에서 소로스 반사성 / Taleb via negativa / López de Prado 다중검증 **팁 한 줄**만 F2에 붙인다. 별도 기능으로 세지 않는다.

## 5. 성공 기준

1. 로그인 없이 첫 화면.
2. 국가를 물으면 출처 있는 숫자 + **양면** 제안.
3. `POST /api/chat` 200 + 비공백. 에이전트가 도구를 탄다 (TRD).
4. §4에 없는 기능을 있는 것처럼 보이지 않는다.

## 6. 비목표

| 하지 않는 것 | 이유 |
|---|---|
| 올웨더/리스크패리티 일변 권고 | 사용자 포지션을 모름. 원전도 양면 |
| 로그인·프로필 | 심사 최저점 |
| 매수/매도, 종목 리포트 | 책임AI. yfinance 주식은 **우선순위 아님** (달리오 범위 밖, Azure에서 Yahoo 차단 위험) |
| 라이브 백테스트·HMM | 5시간·룩어헤드. MLDP는 팁(“대부분 이상치는 거짓”)으로만 |
| IMF·새 Azure·Aspire | 죽거나 감점 |
| 기능 4개 | 대회 제약 |

## 7. 교리 한 줄 (프롬프트용)

- Dalio: 영구기관 = 인과 템플릿. predictor 아님. 금리 ≷ 물가 → 어느 쪽이 유리한지 **번갈아** 나타난다.
- Soros: 가격이 펀더멘털을 바꾸는 구간을 사실로  promot 하지 말 것.
- Taleb: 하지 말 것. 포인트 예측 금지.
- López de Prado (MLDP): 짧은 표본의 예쁜 순위는 다중검증 없이는 광고.

## 변경 이력

| 시각 | 내용 |
|---|---|
| 07:50 | 할 일 정리 앱 초안 |
| 12:15 | Perp_Machine. 양면 제안. 올웨더 기각. 지표 수급표. |
