# PRD — righthon

> 제품 요구사항 (Source of Truth). 기술 설계는 [`TRD.md`](./TRD.md).
> 구현의 최종 진실은 라이브 앱이다. 이 문서에 없는 기능은 존재하지 않는다.
> 작성: 2026-08-22 07:50. **§4 기능만 갱신하면** 당일 추가 요구사항을 반영할 수 있다.

| 항목 | 값 |
|---|---|
| 제품명 | righthon |
| 주제 | 개인 생산성 향상 앱 |
| 라이브 | https://righthon-hale.azurewebsites.net |
| 인증 | **없음. 누구나 URL만으로 즉시 사용 가능** (심사 요건) |
| 현재 기능 수 | **1개** (해커톤 상한 3개) |

## 1. 한 줄 정의

머릿속·메모에 흩어진 **비정형 할 일 텍스트**를 받아, 바로 손댈 수 있는 **실행 가능한 항목**으로 정리해 한국어로 돌려주는 개인 생산성 에이전트.

## 2. 문제 정의

할 일은 이미 있다. 문제는 형태다.

- 채팅, 메모, 머릿속 문장이 **실행 단위로 쪼개지지 않은 채** 쌓인다.
- "내일까지 보고서 쓰고 장 보고 엄마께 전화"처럼 한 덩어리로 적히면, 무엇이 완료인지 보이지 않아 **실행으로 넘어가지 않는다**.
- 기존 할 일 앱은 항목을 *이미 잘 나눈 뒤* 넣도록 강요한다. 정리 자체가 진입 장벽이다.

righthon은 그 정리 한 걸음만 한다. 저장소·캘린더·계정은 이 문제의 핵심이 아니다.

## 3. 타깃 사용자와 시나리오

**1차 사용자.** 할 일을 문장으로 흘려 적는 개인. 목록 앱을 열 기력은 없고, 적힌 것을 실행 단위로 바꾸고 싶다.

**2차 사용자(심사).** 로그인 없이 라이브 URL에 접속해 동작을 확인하는 AI 심사 에이전트. 가입·OAuth·시드 계정은 심사 경로를 끊으므로 **의도적으로 두지 않는다**.

### 시나리오 A — 흩어진 할 일을 실행 목록으로

1. 앱을 연다. 계정 생성·로그인 화면이 없다.
2. textarea에 비정형 텍스트를 적는다. 예: `내일 발표자료 고치고 점심 예약하고 영수증 제출`.
3. **보내기**를 누른다.
4. 모델이 **실행 가능한 항목**으로 정리한 한국어 답을 결과 영역에 표시한다.

### 시나리오 B — 심사 에이전트의 즉시 검증

1. `https://righthon-hale.azurewebsites.net` 에 인증 없이 GET.
2. `POST /api/chat` 에 `{ "message": "..." }` 를 보낸다.
3. HTTP 200과 비어 있지 않은 `reply`(한국어)를 확인한다.

## 4. 기능

기능 상한 3개. 지금은 **F1만 구현**한다. 11:00 이후 요구사항이 공개되면 이 절에 F2·F3를 추가한다.

### F1. 자유 텍스트 → 실행 가능한 항목화 — **현재 구현**

| 항목 | 내용 |
|---|---|
| 입력 | 사용자가 textarea에 적은 자유 텍스트 (`message` 문자열) |
| 처리 | `POST /api/chat` → Azure OpenAI `gpt-5-mini` |
| 시스템 지시 | 입력을 실행 가능한 항목으로 정리해 **한국어로 간결히** 답할 것 |
| 출력 | 결과 영역에 모델 응답(`reply`) 표시 |
| UI | textarea + 보내기 버튼 + 결과 영역. 빌드 없음, 인라인 JS |
| 대화 상태 | **없음.** 요청마다 독립. 서버는 입력을 저장하지 않는다 |

### F2. (예약)

없음. 11:00 요구사항 공개 후 기입.

### F3. (예약)

없음. 11:00 요구사항 공개 후 기입.

## 5. 성공 기준

심사 에이전트가 라이브 URL만으로 아래를 확인하면 성공이다.

1. **즉시 사용.** 로그인·API 키 입력·설치 없이 첫 화면이 열린다.
2. **핵심 가치.** 비정형 할 일 문장을 넣으면 실행 가능한 한국어 항목이 돌아온다.
3. **종단 동작.** `POST /api/chat` 이 HTTP 200을 주고 `reply` 가 비어 있지 않다.
4. **범위 준수.** 이 문서 §4에 적힌 기능만 존재한다. 없는 기능을 있는 것처럼 보이지 않는다.

## 6. 비목표 (Non-goals)

아래는 못 해서가 아니라 **일부러 안 한다.** 기능 1개를 심사 가능한 품질로 끝내는 것이 우선이다.

| 하지 않는 것 | 이유 |
|---|---|
| 로그인·계정·OAuth | 심사 에이전트와 사용자가 즉시 써야 한다 |
| 할 일 저장·동기화·DB | 문제는 "정리"이지 "보관"이 아니다. 서버는 무상태 |
| 멀티턴 대화 히스토리 | 요청마다 독립. 세션·쿠키 없음 |
| 캘린더·알림·메일 연동 | 외부 계정과 추가 Azure 리소스가 필요하다 |
| 스트리밍 응답 | 종단 완성도(200 + 비공백 본문)가 심사 단위다 |
| 파일 업로드·음성 입력 | 현재 입력은 텍스트 한 필드 |
| npm 패키지·새 프레임워크 | 배포 실패 지점을 늘리지 않는다 (`TRD.md` D2) |
| 기능 4개 이상 | 대회 제약(기능 3개 이하) |

## 7. 제약 (제품)

- 제출물은 라이브 Azure URL이다. 로컬 전용 앱은 채점 대상이 아니다.
- 기능은 3개 이하. 15:00–15:40 기능 동결.
- 모델·호스팅은 이미 할당된 Azure 리소스만 사용한다. 새 리소스를 "더 좋아서" 만들지 않는다.

## 8. 향후 확장

11:00 추가 요구사항이 공개되면 **§4에 F2/F3를 채우고** 성공 기준(§5)만 고친다. 후보(미구현, 약속 아님):

- 같은 화면에서 답을 다시 다듬는 후속 입력 (여전히 서버 무상태일 수 있음)
- 출력 형식을 번호 목록으로 고정해 실행 단위를 더 뚜렷하게

계정·DB·새 Azure 서비스는 기본 후보가 아니다. 심사 경로와 배포 단순함을 깨기 때문이다.

## 변경 이력

| 시각 | 내용 |
|---|---|
| 2026-08-22 07:50 | 초안. F1만 기술. |
